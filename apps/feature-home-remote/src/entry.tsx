/**
 * Remote Entry Point
 * 
 * This is the single normalized entry point exposed by the remote.
 * The shell will load this component and render it.
 */
export { default as HomeScreen } from './screens/HomeScreen';

